import { SimpleButtonDirective } from './simple-button.directive';

describe('SimpleButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new SimpleButtonDirective();
    expect(directive).toBeTruthy();
  });
});
